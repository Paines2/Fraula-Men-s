import React from 'react'
import ReactDOM from 'react-dom/client'
import MenuConfigurator from './menu'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <MenuConfigurator />
  </React.StrictMode>,
)